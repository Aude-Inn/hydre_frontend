export interface GameNotificationData {
    _id: string;
    name: string;
    addedAt: string;
  }
  
 export interface MessageData {
    _id: string;
    userId: string;
    userName: string;
    text: string;
    timestamp: string;
  }